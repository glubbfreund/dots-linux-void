conky.config = {
    own_window = true,
    own_window_type = 'desktop',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    own_window_input = false,
    double_buffer = true,
    draw_shades = false,
    draw_outline = false,
    use_xft = true,
    background = true,
    alignment = 'top_right',
    default_color = "#fc9a5d",
    update_interval = 300,
    font = 'Babylonica:size=100',
};

conky.text = [[
${time %A }
${voffset -155}${offset 100}${font Elastre:size=10}${color #393348}${exec date +%-d.\ %B}
]];