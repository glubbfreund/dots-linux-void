#!/bin/bash
# ~/.local/bin/startmodrinth

gpumode=$(~/.local/bin/gpumonitor)
if [[ "$gpumode" == "Hyb" ]];then
    __NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia modrinth-app "$@" &
else
    modrinth-app "$@" &
fi
