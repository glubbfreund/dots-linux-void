#!/bin/bash
# ~/.local/bin/start

if [ -n "$1" ]; then
    ps cax | grep $1 > /dev/null
    if [ $? -eq 0 ]; then
	wmctrl -s $2
    else
	wmctrl -s $2 && $1
    fi
else
    echo "Use start <command> <desktopnumber-1> to open App on specific Desktop."
fi
