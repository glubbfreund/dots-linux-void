#!/bin/bash
# ~/.local/bin/gpumonitor

gpuMode=$(rog-mux -g | awk '{print toupper(substr($NF, 1, 3))}')

if ! lspci | grep -q NVIDIA && [ $gpuMode = "OPT" ]; then
    gpuMode="INT"
fi

echo "$gpuMode"
